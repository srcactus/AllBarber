<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.allbarber">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AllBarber">
        <activity android:name=".home_recuperar_contrasena_barbero"></activity>
        <activity android:name=".home_barbero_scan_qr" />
        <activity android:name=".home_barbero_confirmar_rut" />
        <activity android:name=".home_barbero_cambiar_contrasena" />
        <activity android:name=".home_barbero_agenda" />
        <activity android:name=".home_registro_cliente" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".home_cliente_editar_perfil" />
        <activity android:name=".home_cliente_buscar_barberia" />
        <activity android:name=".home_cliente_solicitar_hora" />
        <activity android:name=".home_cliente_qr" />
        <activity
            android:name=".inicio_sesion_cliente"
            android:label="@string/title_activity_inicio_sesion_cliente"
            android:theme="@style/Theme.AllBarber.NoActionBar" />
        <activity android:name=".home_barbero_perfil" />
        <activity android:name=".home_cliente_perfil" />
        <activity android:name=".inicio_sesion_barbero" />
        <activity android:name=".home_barbero" />
        <activity android:name=".home_cliente" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>